{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run FastAPI server",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-NoProfile",
				"-ExecutionPolicy",
				"Bypass",
				"-Command",
				"cd backend; if (!(Test-Path .venv)) { py -3 -m venv .venv }; .\\.venv\\Scripts\\Activate.ps1; pip install -r requirements.txt; uvicorn app:app --reload --host 0.0.0.0 --port 8000"
			],
			"isBackground": true,
			"problemMatcher": [
				"$tsc"
			],
			"group": "build"
		}
	]
}